// change the start script to match this filepath to run the shortcut run command

const shannon = "shannon"
const bob = "bob"

const items = ['item1', 'item2']

module.exports = { shannon, bob, items }
